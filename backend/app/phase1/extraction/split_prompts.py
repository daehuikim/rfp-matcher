"""조견표 atomic 분해용 few-shot — ① 아래 • 불릿은 한 atom으로 묶는다."""

ATOMIC_UNIT_RULES = """
[atomic 1건의 정의 — PM 검토 단위]
- 분해 경계는 **상위 번호(①②③, 1., 가. 등)** 를 기준으로 한다.
- 상위 번호 **아래** •·- 하위 불릿은 **같은 atom 본문**에 포함한다. 불릿마다 쪼개지 않는다.
- 유사한 하위 항목(같은 ① 주제)은 context window처럼 **한 덩어리**로 묶어도 된다.
- **과분할 금지**: 불릿 1개 = atom 1개 (X). ① + 그 아래 모든 • = atom 1개 (O).
- **과병합 금지**: 서로 다른 ①②③은 합치지 않는다.
"""

SPLIT_SCHEMA_FEW_SHOT = """
[few-shot — 스키마 추론]
입력 샘플:
  ① 원천 시스템 연계
  • 연계 방식은 각 시스템의 요구사항을 반영...
  • 다양한 원천 데이터 소스에 대한 연결...
  ② 지원 파일 형식
  • CSV, Excel...

기대 primary_markers: ["①", "②", "③"]
기대 notes: "①②③에서만 분해. • 불릿은 상위 항목 본문에 포함."
"""

SPLIT_CELL_FEW_SHOT = """
[few-shot — 셀 분해]
입력 셀:
  ① 원천 시스템 연계
  • 연계 방식은 각 시스템의 요구사항을 반영하되, 향후 수집 시스템 확대를 감안하여 다양한 인터페이스를 지원해야 합니다.
  • 다양한 원천 데이터 소스에 대한 연결 및 수집 관리 기능을 제공해야 합니다.
  ② 지원 파일 형식
  • CSV·Excel 등 업로드

기대 atoms (2개):
  1. "① 원천 시스템 연계\\n• 연계 방식은...\\n• 다양한 원천 데이터..."
  2. "② 지원 파일 형식\\n• CSV·Excel..."

잘못된 예 (과분할 — 하지 말 것):
  - atom 3개: ①만 / •연계방식 / •다양한원천  ← 불릿을 각각 atom으로 나눔 (X)
"""
